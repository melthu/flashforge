[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "flashforge"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "rich",
    "readchar",
    "pdfplumber",
    "anthropic",
]

[project.scripts]
flashforge = "flashforge.cli:main"
